node_modules/
.next/
out/
build/

.env
.env*.local

.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store

# ข้อสอบจริง (ลิขสิทธิ์) — เก็บไว้ในเครื่องเท่านั้น ห้าม push
# ใช้เป็นแพตเทิร์นอ้างอิงผ่าน content/exam-analysis.json + content/archetypes/ ที่แปลงแล้ว
ข้อสอบเก่า เคมี ค่าย 1/

# เอกสารสรุปเนื้อหาอ้างอิง (ไฟล์ใหญ่ + ไม่แน่ใจสถานะลิขสิทธิ์) — เก็บไว้ในเครื่องเท่านั้น ห้าม push
# ใช้กลั่นความรู้/แนวโจทย์/แนวรูปมาเขียนใหม่เป็นของเราเท่านั้น ห้ามลอกข้อความ/ตาราง/รูปตรงๆ
# กันพลาดไฟล์อ้างอิงชื่อใหม่ๆ ที่ยังไม่รู้จัก — บล็อก PDF ที่วางไว้ตรงรากโปรเจกต์ทั้งหมด
# (ชีทที่ตั้งใจแจกจริงอยู่ใน public/worksheets/ และ docs/worksheets/pdf/ ไม่ใช่ตรงราก จึงไม่โดนบล็อก)
/*.pdf
