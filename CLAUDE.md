# แอปติวเคมี สอวน. ค่าย 1 — กติกาสำหรับ AI ทุกโมเดลที่ทำงานในโปรเจกต์นี้

**อ่าน [docs/ai-workflow-playbook.md](docs/ai-workflow-playbook.md) ก่อนทำงานผลิต/แก้เนื้อหาเสมอ**
(วิธีทำงานฉบับเต็ม + กับดักที่เคยเจอ) · แผนโปรเจกต์: docs/master-plan.md · บรีฟ: docs/build-brief-chem-camp1-mvp.md

## กติกาที่ห้ามฝ่าฝืน

1. **เนื้อหาทุกชิ้นต้องผ่านไปป์ไลน์ 4 ขั้น**: สร้าง → ตรวจโดยผู้ตรวจอิสระ (สั่งให้หาข้อผิด ไม่ใช่ยืนยัน)
   → แก้ → ตรวจซ้ำจน issues = 0 แล้วปิดด้วย `npm run validate:*` เสมอ
2. **เลขทุกตัวคำนวณด้วย `node -e` ก่อนเขียน** — ทั้งตอนสร้างและตอนตรวจ (ตรวจ = คำนวณใหม่ตั้งแต่ต้นโดยไม่ดูเฉลยก่อน)
3. **LaTeX**: เฉพาะคำสั่งที่ KaTeX รองรับ, **ห้ามอักษรไทยใน math mode**, ใน JSON escape เป็น `\\`
4. **ห้ามลอกข้อสอบจริง** — ออกต้นฉบับตามแนว สอวน. เท่านั้น
5. **ลิงก์ YouTube ห้ามเดา** — ยืนยันผ่าน oembed (`curl youtube.com/oembed?url=...`) ก่อนบันทึกทุกลิงก์
6. **แก้เนื้อหาที่ไฟล์ `content/` เท่านั้น** แล้ว import — ห้ามแก้ข้อมูลตรงใน Supabase
7. **ห้ามเพิ่ม select policy บนตาราง `questions`** — เฉลยต้องไม่ถึงเบราว์เซอร์
   (client ใช้ view `quiz_questions` + RPC `submit_answer`/`topic_stats` เท่านั้น)
8. **Supabase project นี้ใช้ร่วมกับโปรเจกต์อื่น** (มีตาราง `payment`, bucket `divine-cards`)
   — แตะได้เฉพาะ `topics/questions/attempts/contents` · service key ทำ DDL ไม่ได้
   ตารางใหม่ต้องให้ผู้ใช้รัน SQL เอง (เขียนให้ idempotent, copy ใส่คลิปบอร์ดให้)
9. **จบงานต้อง**: `npm run build` ผ่าน + เปิดหน้าเว็บที่แก้ดูจริง + console ไม่มี error
10. ข้อสอบ/บทเรียนเป็น **draft จนกว่าเจ้าของ (แม่นเคมี) รีวิว** — ย้ำเรื่องนี้เมื่อส่งมอบเนื้อหาใหม่

## คำสั่งประจำ

```bash
npm run dev                # dev server (พอร์ตอัตโนมัติ — 3000 ถูกโปรเจกต์อื่นใช้ ห้าม kill)
npm run build              # ต้องผ่านก่อนถือว่าเสร็จ
npm run validate:questions && npm run import:questions -- --replace   # ข้อสอบ (--replace ลบ attempts เดิม)
npm run validate:lessons   && npm run import:lessons                  # บทเรียน (upsert รันซ้ำได้)
```

- ทดสอบเว็บ: บัญชี `test@chemcamp.local` / `testpass123` · ดู LaTeX ที่ `/dev/katex-check`
- ค่าคงที่ตามตารางข้อสอบจริง (ระบุในโจทย์เสมอ): H=1 C=12 N=14 O=16 Na=23 Mg=24 Al=27 Si=28
  P=31 S=32 Cl=35.5 K=39 Ca=40 Fe=56 Cu=63.5 Zn=65 Br=80 Ag=108 I=127 Ba=137 ·
  STP 22.4 L/mol (0°C, 1 atm) · R=0.082 · N_A=6.02×10²³
- Next 16: ใช้ `src/proxy.ts` (ไม่ใช่ middleware.ts) · `params` เป็น Promise · TypeScript pin `^5.9`

## หมวด 8 บท (ตามการวิเคราะห์ข้อสอบจริง 8 ปี — สัดส่วนการออกสอบ)

1. ทักษะพื้นฐานปฏิบัติการเคมี (4%) · 2. โครงสร้างอะตอม (11%) · 3. ตารางธาตุ (14%) ·
4. พันธะเคมี (21%) · 5. โมลและปริมาณสารสัมพันธ์ (28% — หนักสุด) · 6. แก๊ส (8%) ·
7. สารละลาย (12%) · 8. ของแข็งและของเหลว (1.5%)

- ข้อสอบจริง 8 ปี (2560-2568) อยู่ที่ `ข้อสอบเก่า เคมี ค่าย 1/` (PDF + extracted/)
- ผลวิเคราะห์รายข้อ: `content/exam-analysis.json` · แนวข้อต่อบท: `content/archetypes/NN-*.txt`
- **ใช้เป็นแพตเทิร์นอ้างอิงเท่านั้น — ห้ามลอกข้อสอบจริงลงแอปเด็ดขาด** (หลักการข้อ 1 ของ master plan)

## สถานะ (19 ก.ค. 2026) — เดดไลน์เปิดใช้ 31 ก.ค. สอบจริง 30 ส.ค.

ใหม่ล่าสุด: **เฉลยแบบเห็นภาพ — กล่องขั้นตอนสี + ไดอะแกรมประกอบ (บท 1 ครบ 69 ข้อ)**
เจ้าของทดลองให้เด็กดูเฉลยเดิมแล้วไม่ค่อยเข้าใจ ส่งภาพตัวอย่างชีทสรุปฟิสิกส์ยี่ห้ออื่นมาเป็น
แรงบันดาลใจเชิงสไตล์ (ไม่ได้ลอกเนื้อหา/ไดอะแกรม/เลย์เอาต์ตรงจากภาพนั้น):
- **ฝั่งเว็บ**: เพิ่ม rehype plugin เล็กๆ ใน `src/components/MathMarkdown.tsx` (พอร์ต logic
  เดียวกับ `styleCallouts()` ที่มีอยู่แล้วฝั่ง PDF) แปลงป้าย `**หลักคิด:**`/`**ขั้นที่ N**`/
  `**ระวัง:**`/**`**ตอบ:**` ใหม่** ให้เป็นกล่องสี + CSS ใน `src/app/globals.css` — ฝั่ง PDF
  (`scripts/build-worksheet.mjs`) รองรับกล่องสีอยู่แล้วก่อนงานนี้ ไม่ต้องแก้โค้ด
- ธรรมเนียมใหม่บังคับทุกข้อจากนี้ไป: ปิดท้ายเฉลยด้วย `**ตอบ:** ข้อ ก/ข/ค/ง. <คำตอบเต็ม>`
  (กล่องเขียว) แทนประโยคลอย "ดังนั้นคำตอบคือ..."
- วาดไดอะแกรม SVG ใหม่ 10 ไฟล์ใน `public/images/ch01/` (รวมของเดิม 2 ไฟล์ = 12 แบบ):
  สัญลักษณ์ GHS 6 แบบ, เปรียบเทียบอุปกรณ์วัดปริมาตร, การเจือจางกรดปลอดภัย, เป้ายิงความแม่น/
  ความเที่ยง, การอ่านบิวเรต, บันไดหน่วย, แนวปฏิบัติความปลอดภัยในแล็บ, อุปกรณ์ป้องกันส่วนบุคคล,
  ตัวแปรการทดลอง, การ์ดสรุปกฎเลขนัยสำคัญ, สามเหลี่ยมความหนาแน่น, วิธีแฟกเตอร์เปลี่ยนหน่วย —
  ทุกไฟล์ตรวจเรขาคณิตด้วย `getBBox()` ก่อนใช้จริง (screenshot pane เสียอีกรอบเซสชันนี้)
- บท 1 ครบทั้ง 69 ข้อ มีทั้งกล่อง "ตอบ" และไดอะแกรมประกอบ — ผ่านตรวจอิสระ 2 รอบ (นำร่อง 6 ข้อ
  แล้วที่เหลือ 63 ข้อ) พบและแก้แล้ว: (1) ไดอะแกรมบิวเรตเดิมวาดตำแหน่งเมนิสคัสไม่ตรงสเกลตัวเอง
  (2) โจทย์ HCl สไตล์ IChO เดิมมีเลขขั้นกลางไม่สอดคล้องกับค่าที่ปัดแล้วของตัวเอง (125/12.1 ≠ 10.4
  ที่แท้จริงต้องใช้ 12.06 ที่ยังไม่ปัด) — แก้ให้เก็บทศนิยมจนถึงขั้นสุดท้ายแล้ว
- import ขึ้น Supabase แล้ว (`--replace` กระทบทั้ง 8 บท เพราะสคริปต์ import ปัจจุบันไม่รองรับ
  อัปเดตเฉพาะบางข้อในหมวด — เนื้อหาบท 2-8 เหมือนเดิมทุกตัวอักษร แค่แถวในฐานข้อมูลถูกสร้างใหม่)
  regenerate ชีท PDF แล้ว, ทดสอบเว็บจริงผ่าน (login จริง คลิกตอบจริง เห็นรูป+กล่องตอบขึ้นถูก
  console สะอาด), `npm run build` ผ่าน
- เขียนคู่มือกระบวนการ `docs/visual-explanation-design.md` ไว้แล้ว (ธรรมเนียม label, CSS
  mapping, สไตล์ SVG, ตรรกะแม่แบบ vs เฉพาะข้อ, checklist) — **อ่านก่อนทำบทถัดไป**
- **ต่อไป: บท 2 (92 ข้อ)** ทำแบบเดียวกัน — ยังเป็น draft จนกว่าเจ้าของรีวิว

ก่อนหน้า: **บท 2 เสริมอีกรอบจากไฟล์อ้างอิง "สรุปเคมี สอวน.pdf"** (ไฟล์เดียวกับ
"สรุปเนื้อหา เคมี สอวน.pdf" เดิม คนละชื่อ) — ทำ gap analysis เทียบหนังสือบท 7+21 กับที่มีอยู่
(65 ข้อ) พบ 6 หัวข้อควรเพิ่ม เจ้าของอนุมัติทั้ง 6:
- เนื้อหาใหม่ในบทเรียน: ปรากฏการณ์โฟโตอิเล็กทริก, สมมติฐานเดอบรอยล์, หลักความไม่แน่นอน
  ไฮเซนเบิร์ก (แทรกหลังหัวข้อ 4 สเปกตรัม), กฎความเสถียรของนิวเคลียส, อนุกรมการสลายตัว
  กัมมันตรังสี (decay chain), พลังงานยึดเหนี่ยวนิวเคลียส+มวลพร่อง (แทรกท้ายหัวข้อ 8)
- โจทย์ใหม่ 27 ข้อ (`content/questions/02-atomic-structure-quantum-nuclear.json`) — บท 2
  รวมเป็น 92 ข้อ (จาก 65), คลังรวมทั้งแอป 337 ข้อ
- เพิ่ม 6 subtopic ใหม่ใน `content/subtopics.json` + insert ตาราง `subtopics` ใน Supabase
  (topic_id=2) ให้ตรงกัน + เพิ่มเข้า `content/learn-map.json` (ช่วง "สเปกตรัมและแบบจำลองโบร์"
  กับ "กัมมันตภาพรังสีและครึ่งชีวิต" ให้ดึงโจทย์ subtopic ใหม่มาฝึกด้วย)
- ผ่านผู้ตรวจอิสระ (คำนวณซ้ำ ~35 จุดจาก node -e ทั้งหมด) เจอ 1 จุด minor (ช่วง difficulty
  ประกาศไม่ตรงข้อมูลจริง) แก้แล้ว · validate + import ขึ้น Supabase แล้ว · rebuild ชีท PDF
  ทั้ง 8 บทแล้ว (เพราะ build script รันรวมทุกบท) · ทดสอบเว็บจริงผ่าน (lesson/learn/quiz บท 2,
  ลองคำนวณเองข้อ decay chain ข้อแรกตรงกับเฉลยในระบบ, console สะอาด, build ผ่าน)
- **เจอไฟล์ลิขสิทธิ์หลุดเข้า git ระหว่างทาง** (`ติวสบายเคมี (เพิ่มเติม) บทที่ 01...pdf` จากอีกเครื่อง
  ช่วง 16 ก.ค.) — ลบออกจาก git history ทั้งหมดแล้ว (filter-branch + force-push) และแก้
  `.gitignore` เป็น `/*.pdf` กันไฟล์อ้างอิงชื่อใหม่ๆ หลุดซ้ำ — **อีกเครื่องต้อง
  `git fetch origin && git reset --hard origin/main` ครั้งหน้า ไม่ใช่ pull ธรรมดา**

ก่อนหน้า: **ระบบป้ายสไตล์ข้อสอบ + แนวข้อสอบต่างประเทศ** (ตามคำขอเจ้าของ — คุยกันแล้วว่า
**ยังคงแต่งข้อสอบเองทั้งหมด 100% ห้ามเอาโจทย์จริงมาใส่แอป** แค่เพิ่ม "ป้ายสไตล์" บอกแรงบันดาลใจ):
- `content/archetypes-external/{alevel,bmat,icho}-chemistry.txt` — สรุปสไตล์ข้อสอบ A-level/BMAT/
  IChO จากความรู้ทั่วไป (ไม่ใช่ข้อสอบจริง ไม่มีปัญหาลิขสิทธิ์ commit เข้า git ได้ปกติ)
- field `style` ในโจทย์ (ไม่บังคับ) ค่าที่ใช้ได้: "สอวน." "A-level" "BMAT" "IChO" — **ห้ามใส่ปีกำกับ**
  (เช่น "A-level 67") เพราะเป็นการอ้างว่าเป็นข้อจริงปีนั้นซึ่งเราไม่ได้ทำ · validate-questions.mjs
  เช็คแล้ว · แสดง badge เหลืองใน review.html + ชีท PDF (**ยังไม่โชว์ในแอปนักเรียน** ต้องทำ DB
  migration เพิ่มถ้าอยากได้ทีหลัง) · ข้อสอบเดิม 306 ข้อไม่ได้ย้อนหลังติดป้าย
- สาธิตแล้ว 4 ข้อในบท 2 (ข้อละสไตล์ รวมเป็น 65 ข้อ, คลังรวม 310) ผ่านผู้ตรวจอิสระ (เจอ 1 จุด
  คำอธิบายตัวลวงคำนวณย้อนไม่ได้ แก้แล้ว) — import + rebuild ชีทแล้ว
- รายละเอียดกติกา/วิธีใช้ทั้งหมดอยู่ใน `docs/reference-distill-process.md` หัวข้อ "แหล่งแนวข้อสอบ
  ต่างประเทศ" — **อ่านก่อนแต่งข้อสอบสไตล์ใหม่ทุกครั้ง**

ก่อนหน้า: **เสริมบท 2 ครบตามหนังสืออ้างอิง + เขียนกระบวนการกลั่นเป็นเอกสาร**
(1) `docs/reference-distill-process.md` — กระบวนการ 7 ขั้นใช้หนังสืออ้างอิงกับทุกบท (กติกา
ลิขสิทธิ์ ทำได้/ไม่ได้ + แผนที่บทหนังสือ↔บทเรา + เช็คลิสต์) **อ่านก่อนใช้หนังสืออ้างอิงเสมอ**
(2) วิเคราะห์ช่องว่างบท 2 เทียบหนังสือบท 7+21 กับ archetypes/exam-analysis → เติม 7 หัวข้อ:
ลำดับพลังงานคลื่น EM + c=λν · สเปกตรัมเส้น=ลายนิ้วมือธาตุ · สูตร hydrogen-like Z²/n² (ชนแนว
ข้อสอบข้อ 51) · รูปร่างออร์บิทัล s/p/d (+รูปใหม่ orbital-shapes.svg ตรวจภาพจริงแล้ว) ·
para/diamagnetic · โฟโตอิเล็กทริก+เดอบรอยล์ (เกร็ดขอบสนาม) · ฟิชชัน/ฟิวชัน/transmutation
(ชนแนวข้อสอบ 15, 57) — ทุกส่วนเขียนเองผ่านผู้ตรวจอิสระ 0 จุด import + rebuild ชีทแล้ว
ที่ข้ามโดยตั้งใจ (เกินแนวสอบจริง 8 ปี): black-body, พลังงานยึดเหนี่ยว/มวลพร่อง, จลนศาสตร์ ln/k,
magic numbers · **บท 2 ถือว่าครบตามหนังสืออ้างอิงแล้ว — บทถัดไปใช้กระบวนการเดียวกัน**

ก่อนหน้า: **ไฟล์อ้างอิงใหม่ "สรุปเนื้อหา เคมี สอวน.pdf"** (หนังสือ "สรุปเคมีสู่ค่าย สอวน."
โดยณัฐชนน จริยานุรัตน์ 433 หน้า ครอบคลุมก่อนค่าย+ค่าย 1+ค่าย 2 บางส่วน) เจ้าของให้ใช้กลั่น
ความรู้เสริมเนื้อหา/โจทย์/แนวรูป-ตารางของเรา — **สัญญาอนุญาตหน้า 8 เป็น CC BY-NC-ND:
ห้ามลอกข้อความ/ตาราง/รูป/โจทย์ลงแอปตรง ๆ (ND = ห้ามงานดัดแปลง) ใช้ได้แค่สกัดแนวคิด
แล้วเขียนใหม่เองทั้งหมด** แบบเดียวกับข้อสอบเก่า · ไฟล์อยู่ใน .gitignore แล้ว (ห้าม push)
ใช้ครั้งแรก: เทียบบท 7+21 ของหนังสือกับบท 2 ของเรา → เติมช่องว่าง "บีตาบวก/โพซิตรอน +
ทำนายโหมดสลายจาก n/p + ตย. Na-22→Ne-22" ในบทเรียน 02 (เขียนเองทั้งหมด ผ่านผู้ตรวจอิสระ
0 จุด import แล้ว rebuild ชีทแล้ว)

ก่อนหน้า: **แก้เลย์เอาต์พิมพ์ PDF 2 จุดที่เจ้าของทดสอบแล้วเจอจริง**
(1) ตารางยาวเคยบังคับ `break-inside:avoid` ทั้งก้อน → ถ้าไม่พอที่เหลือในหน้าจะเว้นว่างครึ่งหน้า
แล้วดันทั้งตารางไปหน้าใหม่ — แก้เป็นปล่อยตารางไหลข้ามหน้าได้ (`break-inside:auto` + `<thead>
display:table-header-group` ให้หัวตารางซ้ำทุกหน้าที่ตัด + `tr{break-inside:avoid}` กันตัดกลางแถว)
(2) "ตัวอย่างโจทย์ → วิธีทำ → ตอบ" เคยเป็นย่อหน้าอิสระที่ถูกพิมพ์ตัดหน้ากลางคันได้ (โจทย์อยู่หน้าหนึ่ง
วิธีทำอีกหน้า) — เพิ่ม `wrapWorkedExamples()` ใน build-worksheet.mjs สแกนจาก "ตัวอย่างโจทย์" ถึง
กล่อง "ตอบ" (หรือตัวอย่าง/หัวข้อถัดไปถ้าไม่มีกล่องตอบ) ห่อเป็น `<div class="worked-example">`
เดียวกัน + CSS break-inside:avoid — ใช้เฉพาะเนื้อหาบทเรียนผ่าน `renderLessonMd()` ไม่กระทบ
โจทย์/เฉลย (ตรวจว่า "ตัวอย่างโจทย์" เป็น pattern เฉพาะบทเรียนจริง ไม่มีในคำถาม) ตรวจด้วยแคป PDF
จริงยืนยันทั้งสองจุดแล้ว (ch02 หน้า 1-2 ตารางแบบจำลองอะตอมไหลไม่มีหน้าว่าง, หน้า 5-6 ตัวอย่างโจทย์
2.0/2.1 อยู่หน้าเดียวกันครบ) + สุ่มตรวจ ch04 (ตัวอย่างมีสูตรเยอะ) ไม่มีปัญหา — regenerate ครบ 8 บท

ก่อนหน้า: **รีดีไซน์ชีท PDF ทั้งหมด** (`scripts/build-worksheet.mjs` + `export-worksheet-pdf.mjs`)
อ้างอิงแนวชีทติวเตอร์ไทย (ค้นแนวคิด/สีทั่วไปจากเว็บ ไม่ได้ลอกดีไซน์เจ้าใดเจ้าหนึ่ง):
ฟอนต์ Sarabun, แถบหัวบทไล่สีเขียวมรกต+เลขบทวงกลม, กล่องเน้น **หลักคิด/ขั้นที่ (เลขวงกลม)
/ตรวจสอบ/ระวัง/ตอบ** สีต่างกันตามความหมาย, ดาวความยากไล่สีเขียว→แดงตามระดับ, ตารางหัวเขียว
ลายม้าลาย, หัว-ท้ายกระดาษวิ่ง (ชื่อบท + เลขหน้า) ทุกหน้าแบบเอกสารทางการ · **แก้บั๊กจริงระหว่างทาง:
ตารางที่มีสูตรคณิตศาสตร์ในเซลล์เคยเรนเดอร์เป็น "M0/M1" ค้าง** (Markdown table parser ตัดช่องว่าง
รอบ placeholder ทำให้แทนคืนไม่เจอ) — เปลี่ยน placeholder เป็น `@@MATHn@@` ไม่มีช่องว่างแก้แล้ว
ตรวจสอบด้วยการแคป PDF จริงผ่าน pdfjs+puppeteer หลายหน้าทุกบทแล้ว (บั๊กนี้กระทบเฉพาะไฟล์ PDF
ที่ generate เอง ไม่กระทบเว็บแอปจริงซึ่งใช้ remark-math คนละกลไก) — regenerate ครบ 8 บท + push แล้ว

ก่อนหน้า: **โหมดไต่บันได** (`/quiz/[id]?mode=ladder`) + **โหมดเรียน+ฝึก** (`/learn/[id]` สอนสลับฝึก
ทีละช่วงตามแพตเทิร์นหนังสือติว — เนื้อหาจาก contents + โจทย์จับคู่ผ่าน `content/learn-map.json`
ตอนนี้มีเฉพาะบท 2 นำร่อง) — ทั้งคู่ทดสอบเว็บจริง + build ผ่าน

**งานชุด "เรียนแบบติวสบาย" — ทำถึงไหน / ต่ออะไร:**
1. ✅ โหมดเรียน+ฝึก `/learn/2` นำร่อง (learn-map.json + LearnRunner + ปุ่ม 🎓 ใน topics)
2. ✅ รูป SVG บท 2 ครบ 5/5: atomic-models, cathode-ray, gold-foil, h-spectrum, bohr-levels
   (ใน `public/images/ch02/` แทรกใน lesson md + import แล้ว ตรวจเรขาคณิต/โหลดจริงผ่านทั้งหมด
   แต่ **screenshot pane เสียทั้งเซสชัน — เจ้าของช่วยเปิดตาดูความสวยงามอีกรอบ**)
   วิธีทำรูปเพิ่ม: วาด SVG → เปิด /images/chNN/x.svg เช็คตกขอบ/ทับกันด้วย getBBox
   → แทรก `![]()` ใน md → `npm run import:lessons`
3. ✅ ฟีดแบ็กเจ้าของรอบแรก: รูปผ่าน · ซอย learn path เป็น 9 ช่วงสั้น (สอน 1 หัวข้อ→ฝึกทันที
   กันโจทย์ซ้ำข้ามช่วง) · ชีท PDF: `node scripts/build-worksheet.mjs` → `docs/worksheets/ch01-08.html`
   (บทเรียน+แบบฝึกหัดเรียงหัวข้อ ง่าย→ยาก+เฉลยย่อหน้าสุดท้าย เปิดบน iPad → พิมพ์ → บันทึก PDF ได้
   ปุ่ม 🖨️ ในตัว · เฉลยอยู่หน้าสุดท้าย ตัดออกก่อนแจกนักเรียนได้)
3b. ✅ เขียน learn-map ครบทั้ง 7 บทที่เหลือแล้ว (บท 1, 3-8) — ทุกบทเป็นโหมด "สอนสลับฝึก"
   ในทั้ง `/learn/N` และชีท PDF แล้ว ตรวจอิสระยืนยันไม่มี heading ซ้ำ/subtopic ซ้ำระหว่างช่วง
   (เจอบั๊กจริงระหว่างทาง — ### หัวข้อย่อยใต้ ## เดิมทำให้เนื้อหาสอนซ้ำกันหลายช่วงเพราะ
   splitSections() รองรับแค่ระดับ ## แก้โดยยกหัวข้อย่อยที่ต้องแยกฝึกเป็น ## เต็มเสมอ)
3c. ✅ เพิ่มข้อสอบบท 2 อีก 18 ข้อ (extra/extra2 — ผ่านไปป์ไลน์ 4 ขั้น: ผลิต→ตรวจอิสระ→แก้ 2 จุด
   (correct_index กระจุก + เปลี่ยน Br-80→Br-79)→validate 0 error) → บท 2 มี 55 ข้อ คลังรวม 300 ข้อ
   import แล้ว + ชีท/PDF สร้างใหม่แล้ว · **ประเด็นให้เจ้าของตัดสิน (เพิ่ม):** extra2 ข้อ 5 (n=5 นับเส้น
   สเปกตรัม) แนวใกล้ข้อ 9 คลังหลัก (เพิ่มส่วน Balmer ผู้ตรวจว่ารับได้) · extra ข้อ 6 (Li หา% ไอโซโทป)
   แนวเดียว ladder ข้อ 14 ต่างตัวเลข · ถ้ามองว่าฝึกซ้ำแพตเทิร์น = ดี ก็คงไว้ได้
4. ✅ ตรวจความสอดคล้องเนื้อหา↔โจทย์ ch02 (ผู้ตรวจอิสระ) แล้วแก้ครบ: (ก) โค้ดเปลี่ยน pickSpread→
   pickEasiest (ช่วงสอนแรกเคยได้ข้อยากสุดก่อนสอน) (ข) ช่วง 6 เป็นสอนล้วน ฝึกรวมช่วง 7 หลังจบ
   ออร์บิทัล (ค) อุดช่องว่างเนื้อหา 5 จุด: มิลลิแกน+รังสีแคแนล+ตย.คำนวณมวลอิเล็กตรอน (##2),
   สูตรนับเส้นสเปกตรัม n(n-1)/2 (##4), หัวข้อใหม่ ##8 กัมมันตภาพรังสี/ครึ่งชีวิต — ทุกส่วน
   ผ่านผู้ตรวจอิสระ 0 จุดผิด + import แล้ว · PDF: แต่ละช่วงขึ้นหน้าใหม่ (break-before: page)
5. ✅ **เป้าเจ้าของ: ทุกบท ≥60 ข้อ + ความยากไต่ถึงระดับข้อสอบจริง — ครบทุกบทแล้ว (17 ก.ค. 2569)**
   บท 1: 69/60 · บท 2: 92/60 · บท 3: 60/60 · บท 4: 60/60 · บท 5: 60/60 · บท 6: 60/60 ·
   บท 7: 60/60 · บท 8: 60/60 — คลังรวมทั้งแอป **521 ข้อ** ทุกบทผ่านไปป์ไลน์ 4 ขั้น +
   ตรวจอิสระ (คำนวณซ้ำทุกข้อจาก node -e) ก่อน import เสมอ
   บท 6, 7 มีเนื้อหา gap analysis เพิ่มด้วย (บารอมิเตอร์/แมนอมิเตอร์/psi บท 6,
   กฎเฮนรี/ความดันออสโมติก บท 7) · บท 8 ไม่ต้องเพิ่มเนื้อหา (น้ำหนักออกสอบต่ำสุด 1.5%)
   แค่เพิ่มโจทย์+ปรับ subtopic ให้ครอบคลุมทุกหัวข้อ
   บทเรียนจากรอบนี้: กระจาย correct_index (อย่ากระจุก ก), ใช้ไอโซโทป/ค่าจริงที่มีจริง,
   ข้อยากต้องอ้าง archetypes + exam-analysis.json, เช็คซ้ำกับทุกไฟล์ของบทก่อนเขียน ·
   **ห้ามแก้ไฟล์ JSON/MD ที่มี LaTeX ผ่าน `node -e` ใน bash เด็ดขาด** (backslash หลุดหาย
   + `$$` ถูก shell ตีความเป็น process ID มาแล้วจริง ทำให้โจทย์พัง) ใช้ Write/Edit tool
   หรือเขียนสคริปต์ .mjs จริงแล้ว `node run.mjs` เท่านั้น
6. ⬜ ชีท PDF: เมื่อเนื้อหา/ข้อสอบเปลี่ยน รัน `node scripts/build-worksheet.mjs &&
   node scripts/export-worksheet-pdf.mjs` แล้ว copy `docs/worksheets/pdf/*.pdf` →
   `public/worksheets/` (ปุ่มดาวน์โหลดใน topics ชี้ที่นั่น) — บทที่มี learn-map ชีทจะเป็น
   แบบสอนสลับฝึกอัตโนมัติ · ท้ายชีท = เฉลยย่อ 1 หน้า + **เฉลยละเอียดทุกข้อ** (ตามคำขอเจ้าของ)
เอกสารรีวิวสำหรับเจ้าของ: `node scripts/build-review.mjs` → `docs/review/review.html`
(282 ข้อ+เฉลยครบ เรียงง่าย→ยากต่อบท — **มีเฉลย ห้าม deploy/แจก** · อยู่ใน docs/ จึงไม่ถูก Next เสิร์ฟ)

เสร็จ (17 ก.ค. 2569): **ข้อสอบ 521 ข้อ ครบ 8 บท ทุกบท ≥60 ข้อ + learn-map สอนสลับฝึกครบทุกบท**
gap analysis เทียบไฟล์อ้างอิงทำครบทุกบทแล้ว (1,3-8 เสร็จตาม 2 ตามลำดับ) ทุกบทผ่านไปป์ไลน์
4 ขั้น + ตรวจอิสระ 0 issue ก่อน import เสมอ · บทเรียน 8 บท + คลิปยืนยันแล้ว 25 ตัว ·
ทดสอบ end-to-end ผ่านทุกบท (ล็อกอิน + learn/quiz + เฉลย KaTeX เรนเดอร์ถูก + console สะอาด
+ build ผ่าน) · ชีท PDF ทุกบทเป็นแบบสอนสลับฝึกอัตโนมัติแล้ว regenerate + copy ล่าสุดครบ
ค้าง: **เจ้าของ (แม่นเคมี) รีวิวเนื้อหาทั้งหมด — ทุกอย่างยังเป็น draft** (สำคัญที่สุดตอนนี้ —
เนื้อหา/โจทย์ทั้งหมด 521 ข้อ + บทเรียน 8 บทพร้อมให้รีวิวแล้ว) · ประเด็นให้เจ้าของตัดสิน:
ข้อ 11 ใน 05-add แนวใกล้ข้อ 15 คลังเดิม (ผู้ตรวจบอกรับได้) · deploy Vercel ·
ก่อนปล่อยจริงลบ test account + `/dev/katex-check` + rotate service_role key

**กติกาสลับเครื่อง: จบเซสชันให้อัปเดตหัวข้อสถานะนี้ แล้ว commit+push เสมอ** (อีกเครื่อง pull แล้วอ่านต่อได้ทันที)
